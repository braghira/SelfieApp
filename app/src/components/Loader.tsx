import React from 'react';

const Loader: React.FC = () => (
  <div className="flex-center w-full">
    <img src="" alt="loader" width={24} height={24} className="animate-spin"/>
  </div>
);

export default Loader;
